title: BowlineLabs
description: Homelab services

theme: dark
color: slate
headerStyle: clean
statusStyle: dot
startUrl: https://www.bowline.im

quicklaunch:
  searchDescriptions: true
  hideInternetSearch: true
  showSearchSuggestions: true
  hideVisitURL: true
  provider: duckduckgo

layout:
  Media:
    style: row
    columns: 4
  Arr:
    style: row
    columns: 4
  Downloads:
    style: row
    columns: 4
  Productivity:
    style: row
    columns: 4
  Infrastructure:
    style: row
    columns: 4

# Traefik's oidc-auth plugin is the single auth gate; homepage's own
# auth stays off to avoid a second redirect for the same identity.
