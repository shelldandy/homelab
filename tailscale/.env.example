# Tailscale Configuration

# Required - Tailscale authentication key
# Generate one at: https://login.tailscale.com/admin/settings/keys
# Use a reusable key for easy container recreation
TS_AUTHKEY=tskey-auth-your-key-here

# Optional - Subnet routes to advertise (comma-separated)
# Example: Advertise your local network
TS_ROUTES=192.168.1.0/24

# Optional - Extra arguments for tailscale up command
# Common examples:
# - Exit node: --advertise-exit-node
# - Accept subnet routes: --accept-routes
# - Disable key expiry: --auth-key=your-key --authkey-expiry=false
# - Multiple options: --advertise-exit-node --accept-routes
TS_EXTRA_ARGS=--accept-routes

# Optional - Hostname for this Tailscale node
HOSTNAME=homelab-tailscale

# Optional - Use userspace networking (default: false for better performance)
# Set to true if you have issues with kernel networking
TS_USERSPACE=false

# Optional - Accept DNS settings from Tailscale (default: true)
# Set to false if you want to use your own DNS (like Pi-hole/AdGuard)
TS_ACCEPT_DNS=true

# Optional - Run container in privileged mode (default: false)
# Only needed for advanced networking features
TS_PRIVILEGED=false

# Required - Base configuration path (inherited from main .env)
CONFIG_PATH=${HOME}/docker-config